import { expect } from 'chai';
import Item from '../Item';

describe('Item object', () => {
  it('should remove itself after the provided duration');
  it('should cancel the timeout when removed');
});
